// Write a Java program to create a base class Shape with a method called calculateArea(). Create three subclasses:
// Circle, Rectangle, and Triangle. Override the calculateArea() method in each subclass to calculate and
// return the shape's area.
package Polymorphism.Shape;
public class Shape {
    public double calculatedArea() {
        return 0;
    }
}